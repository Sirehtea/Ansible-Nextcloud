# dit bestand zal dienen als een "centraal" variabelenbestand voor alle rollen

# role: change_hostname

change_hostname_desired_hostname: "change_me"

#role: set_static_ip

network_interface: "ens160"  # Change this to your actual network interface name
ip_address: "change_me"  # Change to desired static IP address
subnet_mask: "change_me"  # Change to your subnet mask
gateway: "change_me"  # Change to your gateway IP address
dns_servers: "change_me"  # Change to your DNS server
domain_name: "change_me" # can also be an IP-address

#traefik:

traefik_email: "change_me"

# install_nextcloud sensitive data will be stored in .env
# default condiguration

mysql_root_password: "root"
mysql_password: "root"
mysql_database: "nextcloud"
mysql_user: "nextcloud"

# traefik_requirements

domain_name_traefik: "change_me" # can also be an IP-address